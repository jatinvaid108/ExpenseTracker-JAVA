spring.application.name=admin-service

# MySQL config - use same DB as existing service
spring.datasource.url=jdbc:mysql://localhost:3306/expense_db
spring.datasource.username=root
spring.datasource.password=Harry@29
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

spring.main.web-application-type=servlet

# Admin service runs on different port
server.port=8082

# Simple admin secret for header-based auth (change for production)
admin.secret=change-me-secret
